CREATE TABLE bookings (id SERIAL PRIMARY KEY, user_id INTEGER REFERENCES users(id), name VARCHAR(100) NOT NULL, phone VARCHAR(20) NOT NULL, service VARCHAR(200) NOT NULL, date VARCHAR(20) NOT NULL, time VARCHAR(10) NOT NULL, comment TEXT, status VARCHAR(20) DEFAULT 'new', created_at TIMESTAMP DEFAULT NOW());